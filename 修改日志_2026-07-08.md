# 修改日志

## 基本信息

| 项目 | 内容 |
|------|------|
| **修改人** | 丁紫岚 |
| **修改时间** | 2026-07-08 |
| **修改类型** | Bug 修复 |

---

## 问题描述

普通用户登录后，在主界面点击「退出登录」按钮，页面没有跳转到最初的原始登录页面（`LoginView`），而是跳转到了另一个冗余的登录页面（`ChangePasswordView.showLogin()` 创建的简化版登录窗口，只有手机号+密码输入框）。

**预期行为**：退出后应回到项目启动时的原始登录页面 `LoginView`（包含用户名/密码输入、登录身份选择、记住用户名、显示密码、注册等完整功能）。

**实际行为**：退出后打开了 `ChangePasswordView` 创建的另一个登录窗口（标题为「健康管理系统登录」，仅有手机号和密码字段的简化版）。

---

## 根本原因

`MainView.java` 第 258 行的 `logout()` 方法中，退出登录后的页面跳转代码写的是：

```java
new ChangePasswordView().showLogin();
```

这会创建 `ChangePasswordView` 实例并调用其 `showLogin()` 方法，打开一个不同于原始 `LoginView` 的冗余登录页面。

---

## 解决方案

### 1. 修复 logout() 跳转目标

**文件**: `health-ui/src/main/java/com/healthsys/ui/user/MainView.java`

将 `logout()` 方法中的跳转目标从 `ChangePasswordView` 改为项目原始的 `LoginView`：

```java
// 修改前
new ChangePasswordView().showLogin();

// 修改后
new com.healthsys.ui.auth.LoginView().setVisible(true);
```

### 2. 删除无用登录页面代码

**文件**: `health-ui/src/main/java/com/healthsys/ui/user/ChangePasswordView.java`（已删除）

`ChangePasswordView` 类中的 `showLogin()` 方法是导致跳转错误的冗余登录页面。修复跳转后，该文件不再被任何代码引用，已彻底删除。

---

## 修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `health-ui/src/main/java/com/healthsys/ui/user/MainView.java` | 修改第 258 行 | `logout()` 跳转目标从 `ChangePasswordView` 改为 `LoginView` |
| `health-ui/src/main/java/com/healthsys/ui/user/ChangePasswordView.java` | 删除 | 不再被引用的冗余登录页面代码 |

---

## 解决效果

- 用户点击「退出登录」后，正确跳转回项目原始的 `LoginView` 登录页面，功能完整（包含登录身份选择、记住用户名、显示密码、注册等）
- 删除了无用的冗余登录页面代码，保持代码库整洁
- 不影响其他功能模块

---

## 问题 2：管理员/医生端缺少「退出登录」按钮，退出按钮位置不一致

### 发现时间
2026-07-08（同日第二批修改）

### 问题描述
管理员和医生共用的 `medical.MainView` 侧边栏只有「退出系统」（`System.exit(0)` 直接关闭程序），缺少「退出登录」按钮。且所有按钮（导航+退出）混在同一列表中，与用户端「导航在上、弹性空间居中、退出按钮在底部」的布局不一致。

### 根本原因
原侧边栏将所有按钮放在一个 `String[]` 数组中统一循环创建，未区分导航功能和退出功能，也未添加 `Box.createVerticalGlue()` 将退出按钮推至底部。

### 解决方案

1. **从导航数组中移除「退出系统」**，使其不再混入导航按钮循环。
2. **新增「退出登录」按钮**，点击后 dispose 父窗并打开 `LoginView`，与用户端行为一致。
3. **「退出系统」独立创建**，放在「退出登录」下方。
4. **添加 `Box.createVerticalGlue()`** 将退出按钮推至侧边栏底部。
5. **抽取 `createNavButton()` 方法**，消除按钮创建的重复代码。
6. **新增 `getParentFrame()` 方法**，从 `JPanel` 向上查找父 `JFrame` 以便退出登录时关闭窗口。

### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `health-ui/src/main/java/com/healthsys/ui/medical/MainView.java` | 修改 | 新增「退出登录」按钮、退出按钮移至底部、代码重构 |

### 解决效果
- 管理员/医生端与用户端退出按钮布局统一：导航按钮在上，退出按钮在底部
- 管理员/医生可通过「退出登录」回到原始登录页面，无需重新启动程序
- 「退出系统」保留，用于直接关闭程序

---

## 问题 3：管理员/医生端退出登录缺少确认弹窗

### 发现时间
2026-07-08（同日第三批修改）

### 问题描述
用户端点击「退出登录」有确认询问窗口（"确定要退出登录吗？"），但管理员/医生端直接退出，缺少确认步骤，与用户端行为不一致。

### 解决方案
在 `medical.MainView` 的「退出登录」按钮事件中添加 `JOptionPane.showConfirmDialog` 确认弹窗，与用户端一致。

### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `health-ui/src/main/java/com/healthsys/ui/medical/MainView.java` | 修改 | 退出登录添加确认弹窗 |

### 解决效果
管理员/医生端退出登录时同样弹出确认窗口，与用户端行为统一。

---

## 问题 4：用户端预约时间选择器改用日历控件

### 发现时间
2026-07-08（同日第四批修改）

### 问题描述
用户端预约模块的时间选择使用 `JSpinner` + `SpinnerDateModel`，默认按「天」步进，点击箭头只能改日期，无法方便地选择小时和分钟，需要手动打字输入时间，体验较差。

### 解决方案
将 `JSpinner` 替换为 `JDateChooser`（项目已有 jcalendar 依赖），弹窗改为：

- **日历控件**（`JDateChooser`）：可视化选择日期
- **时间下拉框**（`JComboBox`）：提供 08:00~17:00 每半小时时段选择

与医疗端 `AppointmentDialog` 保持一致风格。

### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `health-ui/src/main/java/com/healthsys/ui/user/AppointmentView.java` | 修改 | showTimeSelectionDialog 改用 JDateChooser + 时间下拉框 |
| `health-ui/src/main/java/com/healthsys/ui/user/MessagesView.java` | 修改 | showTimeSelectionDialog 改用 JDateChooser + 时间下拉框 |

### 解决效果
- 用户可通过日历控件直观选择预约日期
- 通过下拉框快速选择时段，无需手动输入
- 与医疗端预约界面风格统一

---

## 问题 5：预约按钮颜色过浅 + 缺少过去时间校验

### 发现时间
2026-07-08（同日第五批修改）

### 问题描述
- 「确认预约」按钮颜色偏浅（原 `Color(70,104,197)`），白色文字在浅蓝底上不够醒目
- 预约时间选择未校验是否为过去时间，用户可以预约已过时的时间段

### 解决方案
- 按钮颜色调整为 `Color(41,75,166)`（深蓝），字号增大到 16px 加粗，添加 `setFocusPainted(false)` 和内边距
- 提交前校验：`ldt.isBefore(LocalDateTime.now())`，若为过去时间则提示「不能预约过去的时间」

### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `health-ui/src/main/java/com/healthsys/ui/user/AppointmentView.java` | 修改 | 按钮颜色加深 + 过去时间校验 |
| `health-ui/src/main/java/com/healthsys/ui/user/MessagesView.java` | 修改 | 按钮颜色加深 + 过去时间校验 |

### 解决效果
- 按钮视觉效果更醒目，文字清晰可读
- 用户无法选择过去的日期时间，避免无效预约

---

## 问题 6：预约界面缺少刷新按钮

### 发现时间
2026-07-08（同日第六批修改）

### 问题描述
用户创建新的检查组后，预约列表中不会自动显示，必须退出登录重新进入才能看到新建的数据。

### 解决方案
在预约页面工具栏添加「刷新」按钮，点击调用已有的 `refreshAppointmentData()` 方法重新加载数据。

### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `health-ui/src/main/java/com/healthsys/ui/user/AppointmentView.java` | 修改 | 工具栏添加刷新按钮 |

### 解决效果
点击刷新按钮即可重新加载最新预约数据，无需退出重新登录。

---

## 问题 7：修正刷新按钮位置 — 预约界面（MessagesView）添加刷新

### 发现时间
2026-07-08（同日第七批修改）

### 问题描述
上一轮修改将刷新按钮误加到了 `AppointmentView`（「消息」按钮对应的页面），而用户所指的「预约界面」实际是侧边栏「预约」按钮对应的 `MessagesView`（检查组列表页）。

### 解决方案
在 `MessagesView` 标题栏添加「刷新」按钮，调用新增的 `refreshGroupData()` 方法重新加载检查组数据。

### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `health-ui/src/main/java/com/healthsys/ui/user/MessagesView.java` | 修改 | 标题栏添加刷新按钮 + refreshGroupData() 方法 |

### 解决效果
在预约界面（检查组列表）点击刷新即可看到新建的检查组，无需退出重进。

---

## 问题 8：ExamRecordView 数组越界崩溃

### 发现时间
2026-07-08（同日第八批修改）

### 问题描述
普通用户登录后，`MainView` 初始化 `ExamRecordView` 时崩溃：
```
java.lang.ArrayIndexOutOfBoundsException: Index 1 out of bounds for length 1
    at ExamRecordView.loadExamRecords(ExamRecordView.java:81)
```
原因是数据库 `check_items` 表中 `normal_range` 字段取值格式不统一，部分记录只有数值不含 `:` 分隔符，原代码 `split(":")[1]` 直接取下标 1 导致越界。

### 解决方案
`split(":")` 后先判断数组长度 `> 1` 再取 `[1]`，同时增加 `normalRange` 的 null 检查。

### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `health-ui/src/main/java/com/healthsys/ui/user/ExamRecordView.java` | 修改 | split 前加长度判断 + null 检查 |

### 解决效果
无论 `normal_range` 字段是否包含 `:`，均不再崩溃。

---

## 问题 9：新注册用户登录无反应

### 发现时间
2026-07-08（同日第九批修改）

### 问题描述
新注册用户点击登录按钮后没有任何反应，不报错也不跳转。

### 根本原因
1. 注册时未设置 `firstLogin = false`，默认值为 `true`
2. 登录时 `AuthService` 检测到 `firstLogin = true` 调用 `onFirstLogin` 回调
3. `LoginView` 的 `onFirstLogin` 是空方法（之前由已删除的 `ChangePasswordView` 处理首次登录改密流程）

### 解决方案
1. `RegisterView` 注册时设置 `newUser.setFirstLogin(false)`，新用户无需改密
2. `LoginView` 的 `onFirstLogin` 改为与 `onLoginSuccess` 相同逻辑，兼容数据库中已有的 `firstLogin = true` 旧用户

### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `health-ui/src/main/java/com/healthsys/ui/auth/RegisterView.java` | 修改 | 注册时设置 firstLogin = false |
| `health-ui/src/main/java/com/healthsys/ui/auth/LoginView.java` | 修改 | onFirstLogin 改为跳转成功路径 |

### 解决效果
新注册用户可正常登录进入主界面，旧用户也不受影响。

---

## 问题 10：用户端导航映射名实不符

### 发现时间
2026-07-08（同日第十批修改）

### 问题描述
用户端 `MainView` 中 panel ID 与导航按钮标签之间存在映射错乱：
- "预约"按钮调用 `showPanel("messages")`，但 `"messages"` 实际是 MessagesView（检查组列表页）
- "消息"按钮调用 `showPanel("appointment")`，但 `"appointment"` 实际是 AppointmentView（预约记录页）
- `updateWindowTitle` 中 `"messages"`→"消息"（应为"预约"）、`"appointment"`→"自定义检查组"（应为"我的预约"）
- `getCurrentViewName` 中 `"messages"`→"预约"、`"appointment"`→"消息"，与标题映射互相矛盾
- `customPackage` 在两个 switch 中缺失，导致切换到自定义检查组时标题栏和状态栏显示空字符串
- `userManagement` 分支为死代码，无对应 panel

### 解决方案
统一重命名 panel ID 使其与实际内容一致，并修正所有关联的 switch 映射：

| Panel ID | 修改前 | 修改后 | 对应视图 |
|----------|--------|--------|----------|
| MessagesView | `"messages"` | `"groups"` | 检查组列表（预约入口） |
| AppointmentView | `"appointment"` | `"appointments"` | 预约记录列表 |

导航按钮"消息"→"我的预约"，反映实际页面内容。

`updateWindowTitle` 和 `getCurrentViewName` 的 switch 同步修正，补全 `customPackage` 分支，删除无用的 `userManagement` 分支。

### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `health-ui/src/main/java/com/healthsys/ui/user/MainView.java` | 修改 | 重命名 panel ID、修正按钮标签、补全 switch 分支 |

### 解决效果
- 导航按钮标签与 panel ID 语义一致，代码可读性提升
- 窗口标题和状态栏在所有页面切换时均正确显示
- 消除了两个互相矛盾的 switch 映射

---

## 2026-07-09 综合修改（慈曾华）

### 一、新功能

#### 1.1 "我的预约"详情按钮
- **文件**: `AppointmentView.java`、`AppointmentDetailDialog.java`（新增）
- **内容**: 表格第5列"状态"改为蓝色"详情"按钮，点击弹出明细窗口
- 明细窗口展示：套餐名称/描述/日期/时段 + 检查项目表格（序号、项目名、科室、参考值、单价）+ 费用汇总

#### 1.2 打印预览报告（Word 导出）
- **文件**: `AppointmentView.java`、`WordExportService.java`（新增）
- **内容**: 右上角添加"打印报告"按钮，选中预约后导出 .docx 文件
- 文档包含：预约信息 + 检查项目明细表格 + 费用汇总
- **依赖**: Apache POI（poi-ooxml 5.2.5）

#### 1.3 打印支持"全部"和"单条"选择
- **文件**: `AppointmentView.java`、`WordExportService.java`
- **内容**: 点击"打印报告"弹出选择对话框
  - "打印全部预约"→ 按顺序导出所有预约到一份合并 Word
  - "打印选中预约"→ 仅导出当前选中记录

#### 1.4 BCrypt 密码加密
- **文件**: `PasswordUtil.java`（新增）、`AuthService.java`、`UserDAO.java`、`AdminDAO.java`、`DoctorDAO.java`、`UserService.java`、`RegisterView.java`
- **内容**: 密码存储改为 BCrypt 哈希
  - 新注册/改密：自动 BCrypt 哈希
  - 旧密码登录：明文匹配成功后自动无缝迁移为 BCrypt
  - 依赖：jbcrypt 0.4

### 二、Bug 修复

#### 2.1 预约创建缺少 created_at 字段
- **文件**: `AppointmentDAO.java`
- **问题**: `createAppointment()` INSERT 语句漏写 `created_at`，导致新预约创建时间为 NULL
- **修复**: 补齐 `created_at` 列和参数

#### 2.2 检查组事务方法连接泄漏
- **文件**: `CheckItemGroupDAO.java`
- **问题**: `updateGroup()`、`delete()`、`createGroup()` 三个方法的 finally 块未关闭连接
- **修复**: finally 块添加 `conn.close()`

#### 2.3 支付流程死代码
- **文件**: `AppointmentView.java`
- **问题**: `if(true)` 模拟支付块导致真实支付流程被跳过
- **修复**: 删除 `if(true)` 块，保留完整支付确认 → 付款 → 更新状态流程

#### 2.4 "详情"按钮 3 次重复查询数据库
- **文件**: `AppointmentView.java`、`AppointmentService.java`
- **问题**: 点击一次"详情"会查 3 遍同一个 appointment
- **修复**: 查一次复用 groupId，新增 `getCheckItemsByGroupId()` 方法

#### 2.5 MessagesView 预约后缺少支付确认
- **文件**: `MessagesView.java`
- **问题**: 检查组信息页面预约成功后不弹支付确认，AppointmentView 有
- **修复**: 补上支付确认弹窗

#### 2.6 空检查组显示空白详情
- **问题**: check_groups 表中 ID 11-20 为重复空壳（无检查项），点击"查看详情"弹出空白窗口
- **修复**: 数据库层面补齐 groups 11-20 的 group_item_relation 关联数据

#### 2.7 MainView 创建 null groupId 的 dummy Appointment
- **文件**: `MainView.java`
- **问题**: `new Appointment(userId, null, ...)` 可能导致 ExamRecordView 中的 NPE
- **修复**: 改为 `0L`

### 三、排序优化

#### 3.1 预约列表按 ID 排序
- **文件**: `AppointmentDAO.java`
- **改动**: `getUserAppointments` 的 `ORDER BY` 从 `appointment_time DESC` → `appointment_id ASC`
- **效果**: 刷新后按创建顺序排列，不乱序

#### 3.2 检查组列表按 ID 排序
- **文件**: `CheckItemGroupDAO.java`
- **改动**: `getAll()` 的 `ORDER BY` 从 `group_name` → `group_id ASC`
- **效果**: 刷新后检查组信息按 ID 顺序排列

### 四、代码质量

#### 4.1 消除硬编码 `getComponent(1)`
- **文件**: `AppointmentView.java`（7处）、`MessagesView.java`（1处）
- **问题**: 通过组件树索引获取 JTable，改布局就崩溃
- **修复**: 改为成员变量 `appointmentTable`/`tableModel` 和 `groupTable`/`groupModel`

#### 4.2 N+1 查询优化
- **文件**: `AppointmentView.java`、`MessagesView.java`
- **问题**: `loadAppointmentData` 查全表检查组再内存过滤、`MessagesDetailButtonEditor` 每次重查全表
- **修复**: 改为 `getCheckItemGroupById()` 精确查询

#### 4.3 Maven groupId 统一
- **文件**: 所有 `pom.xml`
- **改动**: `com.ncu.wan` → `com.healthsys`（与 Java 包名一致）

#### 4.4 移除 DbUtil 硬编码数据库凭证
- **文件**: `DbUtil.java`
- **问题**: 配置缺失时硬编码 root/空密码回退
- **修复**: 配置缺失直接抛 RuntimeException

#### 4.5 时区一致性
- **文件**: `AppointmentService.java`
- **改动**: `ZoneId.systemDefault()` → `ZoneId.of("UTC")`

### 五、UI 调整

#### 5.1 按钮文字颜色统一
- **文件**: `MainView.java`、`AppointmentView.java`、`AppointmentDetailDialog.java`、`MessagesView.java`
- **改动**: 导航按钮、打印按钮、确认预约按钮、关闭按钮的文字统一为黑色加粗

### 修改文件汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| `pom.xml` | 修改 | groupId + poi-ooxml + jbcrypt 依赖 |
| `health-common/pom.xml` | 修改 | groupId + jbcrypt 依赖 |
| `health-dao/pom.xml` | 修改 | groupId |
| `health-service/pom.xml` | 修改 | groupId |
| `health-ui/pom.xml` | 修改 | groupId + poi-ooxml 引用 |
| `DbUtil.java` | 修改 | 移除硬编码凭证 |
| `PasswordUtil.java` | **新增** | BCrypt 密码工具 |
| `AppointmentDAO.java` | 修改 | created_at + 排序 |
| `CheckItemGroupDAO.java` | 修改 | 连接泄漏 + 排序 |
| `AdminDAO.java` | 修改 | updatePasswordHash |
| `DoctorDAO.java` | 修改 | updatePasswordHash |
| `UserDAO.java` | 修改 | updatePasswordHash |
| `AuthService.java` | 修改 | BCrypt 验证 + 自动迁移 |
| `AppointmentService.java` | 修改 | 时区 + getCheckItemsByGroupId |
| `UserService.java` | 修改 | 密码 BCrypt |
| `RegisterView.java` | 修改 | 注册密码 BCrypt |
| `AppointmentView.java` | 修改 | 详情按钮 + 打印 + 排序 + getComponent(1) + 支付修复 |
| `AppointmentDetailDialog.java` | **新增** | 预约详情弹窗 |
| `WordExportService.java` | **新增** | Word 导出服务 |
| `MainView.java` | 修改 | null groupId + 按钮颜色 |
| `MessagesView.java` | 修改 | 支付确认 + getComponent(1) + 排序 + 按钮颜色 |
| `CheckGroupDialog.java` | 修改 | import 冲突修复 |
| `init_database.sql` | 修改 | 远程更新 +331 行 |
